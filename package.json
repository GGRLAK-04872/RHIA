{
  "name": "rhia",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:node && npm run test:worker && npm run check:memory-boundaries",
    "test:node": "node --test tests/*.test.mjs",
    "test:worker": "vitest run --config vitest.config.js",
    "check:memory-boundaries": "node scripts/check-memory-boundaries.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  },
  "engines": {
    "node": ">=20"
  }
}
